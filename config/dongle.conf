# ── Dongle Config ─────────────────────────────────────────────────────────────
# The dongle is the USB central. Both halves are BLE peripherals.

# Split: act as central for 2 peripherals (left + right)
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_BLE=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS=2

# USB HID — this is how the dongle talks to your PC
CONFIG_ZMK_USB=y

# No deep sleep — dongle is USB powered
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=0

# ── ZMK Studio ────────────────────────────────────────────────────────────────
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n

# ── Display & Bongo Cat ───────────────────────────────────────────────────────
CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_WIDGET_BATTERY_STATUS=y
CONFIG_ZMK_WIDGET_WPM_STATUS=y
CONFIG_ZMK_WIDGET_LAYER_STATUS=y
CONFIG_ZMK_WIDGET_OUTPUT_STATUS=y

# Improves display response
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_BUILTIN=y
CONFIG_ZMK_LV_FONT_DEFAULT_SMALL_MONTSERRAT_26=y
